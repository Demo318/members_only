<h1>Index of Posts</h1>

<% @posts.each do |p| %>
  <h2>
    <%= p.title %>
  </h2>
  <p>
    <%= p.body %>
  </p>
  <% if user_signed_in? %>
    <p>
      <%= p.user.username %>
    </p>
  <% end %>
<% end %>